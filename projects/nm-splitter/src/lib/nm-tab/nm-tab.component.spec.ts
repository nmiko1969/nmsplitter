/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NmTabComponent } from './nm-tab.component';

describe('NmTabComponent', () => {
  let component: NmTabComponent;
  let fixture: ComponentFixture<NmTabComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NmTabComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(NmTabComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
