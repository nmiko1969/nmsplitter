/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NmSplittertabComponent } from './nm-splittertab.component';

describe('NmSplittertabComponent', () => {
  let component: NmSplittertabComponent;
  let fixture: ComponentFixture<NmSplittertabComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NmSplittertabComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NmSplittertabComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
